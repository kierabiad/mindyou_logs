services:
  docs:
    image: mindyou_logs_local_docs
    container_name: mindyou_logs_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./mindyou_logs:/app/mindyou_logs:z
    ports:
      - '9000:9000'
    command: /start-docs
